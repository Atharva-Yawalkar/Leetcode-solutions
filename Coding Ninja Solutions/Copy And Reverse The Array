#include <bits/stdc++.h> 
vector < int > copyAndReverse(vector < int > arr, int n) {
    vector < int > copyArr(n);
    for (int i = 0; i < n; i++) {
        copyArr[n - i - 1] = arr[i];
    }
    return copyArr;
}	
