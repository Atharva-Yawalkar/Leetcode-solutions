class Solution {
public:
    void rotate(vector<int>& v, int k) {
        int r =k%v.size();
        reverse(v.begin(),v.end());
        reverse(v.begin(),v.begin()+r);
        reverse(v.begin()+r,v.end());
    }
};
